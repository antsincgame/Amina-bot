<!-- external.xml — SIP профиль для подключения к LiraX АТС -->
<profile name="external">
  <gateways>
    <!-- Шлюз к LiraX PBX -->
    <gateway name="lirax">
      <param name="username" value="$${lirax_sip_login}"/>
      <param name="password" value="$${lirax_sip_password}"/>
      <param name="realm" value="$${lirax_sip_server}"/>
      <param name="proxy" value="$${lirax_sip_server}"/>
      <param name="register" value="true"/>
      <param name="register-transport" value="udp"/>
      <param name="expire-seconds" value="300"/>
      <param name="retry-seconds" value="30"/>
      <param name="caller-id-in-from" value="false"/>
      <param name="ping" value="25"/>
    </gateway>
  </gateways>

  <settings>
    <param name="sip-ip" value="$${local_ip_v4}"/>
    <param name="sip-port" value="5080"/>
    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="ext-rtp-ip" value="$${external_rtp_ip}"/>
    <param name="ext-sip-ip" value="$${external_sip_ip}"/>

    <param name="codec-prefs" value="$${global_codec_prefs}"/>
    <param name="inbound-codec-negotiation" value="generous"/>

    <!-- Контекст для входящих звонков от LiraX -->
    <param name="context" value="public"/>

    <!-- NAT -->
    <param name="aggressive-nat-detection" value="true"/>
    <param name="NDLB-received-in-nat-reg-contact" value="true"/>

    <!-- RTP -->
    <param name="rtp-timeout-sec" value="300"/>
    <param name="rtp-hold-timeout-sec" value="1800"/>
  </settings>
</profile>
